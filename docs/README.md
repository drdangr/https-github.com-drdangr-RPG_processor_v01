# AI RPG State Processor (Game Master Module)

## Обзор
Это веб-приложение (React + TypeScript) для управления состоянием настольной ролевой игры с использованием ИИ (Google Gemini). Приложение выступает в роли "State Processor" (Процессора Состояний).

Оно позволяет Гейм-Мастеру (или соло-игроку):
1. Хранить и редактировать состояние мира (Локации, Игроки, Предметы) в удобном UI.
2. Вводить текстовые заявки (действия) игроков.
3. Отправлять состояние и заявку в Gemini API.
4. Автоматически обрабатывать изменения состояния через **Инструменты (Tools)** (перемещение объектов, создание/удаление, изменение атрибутов).
5. Получать художественное повествование и обновленный JSON состояния.
6. **Просматривать "мысли" модели** (thinking mode) — рассуждения ИИ перед принятием решений.

## Стек технологий
*   **Frontend:** React 19, TypeScript
*   **Styling:** Tailwind CSS (inline classes)
*   **AI:** @google/genai SDK (Gemini 2.5 Flash с поддержкой thinking)
*   **Build:** Vite 6
*   **Testing:** Vitest

## Установка и Запуск

1.  **Установите зависимости:**
    ```bash
    npm install
    ```

2.  **Настройте API Key:**
    Приложение ожидает ключ API Google Gemini в переменной окружения `process.env.API_KEY`.
    Создайте файл `.env` в корне проекта:
    ```
    API_KEY=ваш_ключ_здесь
    ```

3.  **Запустите сервер разработки:**
    ```bash
    npm run dev
    ```
    Приложение будет доступно по адресу `http://localhost:3000/`

4.  **Запустите тесты:**
    ```bash
    npm test
    ```

5.  **Сборка для продакшена:**
    ```bash
    npm run build
    npm run preview
    ```

## Основные возможности

### Редактирование данных
*   Визуальные формы для редактирования JSON-структур
*   Выпадающие списки для связей (ID локаций, игроков, объектов)
*   Редактор атрибутов с динамическим добавлением/удалением

### Система инструментов
*   **5 инструментов:** move_object, set_attribute, delete_attribute, create_object, delete_object
*   Многоходовый цикл — AI может вызывать инструменты последовательно (до 5 итераций)
*   Включение/отключение инструментов в UI

### Результаты симуляции
*   **Повествование** — художественный текст от AI
*   **💭 Мысли модели** — раскрывающийся блок с рассуждениями AI (thinking mode)
*   **Лог инструментов** — группировка по шагам (итерациям)
*   **Дифф-Viewer** — визуализация изменений (было → стало)

### Сохранение
*   Автосохранение через 2 секунды после изменений
*   Сохранение через API (файлы `data/*.ts`) или localStorage
