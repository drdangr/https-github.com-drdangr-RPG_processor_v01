# Планируемые улучшения системы

Этот документ содержит список улучшений, которые стоит реализовать для повышения качества и производительности системы Симуляция + Нарратив.

## Исправлено ✅

### 1. Язык thinking

- **Проблема**: Модель размышляла на английском языке
- **Решение**: Добавлена инструкция `ЯЗЫК: Думай и отвечай на русском языке` в оба системных промпта

### 2. Валидация аргументов инструментов

- **Проблема**: Отсутствовала проверка обязательных параметров перед вызовом инструмента
- **Решение**: Добавлена валидация `required` параметров в `geminiService.ts`

### 3. Дублирование кода обработки tool calls

- **Проблема**: Код выполнения инструментов дублировался в двух местах (основной цикл и обработка оставшихся tool calls)
- **Решение**: Вынесена функция `executeToolCalls` в `services/geminiService.ts`, которая централизует логику выполнения инструментов и устраняет дублирование кода

### 6. Большой JSON состояния в каждом запросе

- **Проблема**: Весь `GameState` сериализовался в каждый запрос, что значительно увеличивало расход токенов при каждом шаге многоходового цикла
- **Решение**: Реализована функция `getRelevantState`, которая возвращает только релевантные данные:
  - Текущая локация игрока (с полным описанием)
  - Соседние локации (только ID, name и connections, без описаний)
  - Объекты в текущей локации и у игрока
  - Объекты в соседних локациях и вложенные объекты
  - Для финального нарратива используется полное состояние (нужно для разметки всех объектов)
- **Эффект**: Значительное снижение расхода токенов на каждом шаге симуляции (экономия может достигать 50-70% в зависимости от размера мира)

### 13. Улучшение UX выбора связей (Hierarchy Chips)

- **Проблема**: Сложно выбирать родительский объект в выпадающем списке.
- **Решение**: Реализован компонент `HierarchySelect`, отображающий путь к объекту в виде цепочки кликабельных "чипсов" (Breadcrumbs).

### 14. Drag and Drop для объектов

- **Проблема**: Неудобно перемещать объекты между контейнерами через выпадающие списки.
- **Решение**: Добавлена поддержка Drag and Drop в списке объектов. Поддерживается перемещение с вложенными структурами и защита от циклических ссылок.

### 12. Unit-тесты для geminiService

- **Проблема**: Отсутствовали тесты на ключевую логику симуляции.
- **Решение**: Добавлены comprehensive тесты в `services/__tests__/geminiService.test.ts` (покрытие: обработка текста, тул-коллы, циклы, ошибки).

### 15. Поддержка ссылок в аргументах инструментов ($N.createdId)

- **Проблема**: При создании цепочек действий (создать объект -> переместить его) второй инструмент не знал ID только что созданного объекта.
- **Решение**: Реализована система резолвинга ссылок `resolveReferences` в `geminiService.ts`. Поддерживает синтаксис `$0.createdId`, `$1.createdId` для использования результатов предыдущих инструментов в той же итерации.

### 16. Поддержка Thinking Models

- **Проблема**: Сложная логика симуляции требовала рассуждений модели перед действием.
- **Решение**: Добавлена поддержка параметров `thinkingConfig` и извлечение `thought` частей из ответа модели (для моделей с поддержкой thinking, например `gemini-2.0-flash-thinking`).

### 10. Вынести промпты в отдельный файл

- **Проблема**: Системные промпты были зашиты в `services/geminiService.ts`, что усложняло редактирование.
- **Решение**: Промпты вынесены в `prompts/systemPrompts.ts` и импортируются в сервис.

### 4. Нет информации о локации игрока в контексте нарратива

- **Проблема**: Нарратив получал только `userPrompt`, но не знал, где находится игрок (атмосфера, описание локации).
- **Решение**: В `createSystemInstruction` добавлен блок, который находит текущую локацию игрока и добавляет её описание в контекст нарратива.

### 5. Нет истории действий в нарративном запросе

- **Проблема**: Нарратив получал только историю диалога, но не знал о механических результатах действий (урон, изменения состояния).
- **Решение**: В историю ходов (`historySection`) добавлены `toolLogs`, отображающие результаты выполнения инструментов.

### 7. Нет retry логики

- **Проблема**: При сбоях сети (503, 429) запросы сразу падали.
- **Решение**: Добавлен `utils/retry.ts` с экспоненциальной задержкой. Все вызовы API обернуты в `withRetry`.

### 8. Нет таймаутов

- **Проблема**: Запросы могли висеть бесконечно.
- **Решение**: Добавлен `AbortController` и 30-секундный таймаут для всех вызовов API.

### 9. Нет кэширования системных инструкций

- **Проблема**: Тяжелые промпты генерировались заново при каждом вызове.
- **Решение**: Добавлена мемоизация `createSystemInstruction` с использованием хеша состояния и LRU-кэшем.

### 11. Типизация ответов API

- **Проблема**: Ответы от Gemini API типизировались как `any`, что приводило к ошибкам доступа к свойствам.
- **Решение**: Введен строгий интерфейс `GeminiApiResponse` в `types.ts`, все методы сервиса переведены на строгую типизацию.

---

## Низкий приоритет 🟢

---

## Как добавлять новые улучшения

1. Добавьте описание проблемы
2. Укажите рекомендуемое решение с примером кода
3. Укажите файлы, которые нужно изменить
4. Присвойте приоритет (🔴 высокий, 🟡 средний, 🟢 низкий)
5. После реализации переместите в секцию "Исправлено ✅"
